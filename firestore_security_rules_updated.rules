rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // Helper function to check if user is authenticated
    function isAuthenticated() {
      return request.auth != null;
    }
    
    // Helper function to check if user owns the resource
    function isOwner(userId) {
      return request.auth.uid == userId;
    }
    
    // Users collection - users can only access their own data
    match /users/{userId} {
      allow read, write: if isAuthenticated() && isOwner(userId);
      
      // Daily points subcollection - CRITICAL for daily points functionality
      match /dailyPoints/{date} {
        allow read, write: if isAuthenticated() && isOwner(userId);
      }
      
      // Daily activities subcollection - CRITICAL for daily activities functionality
      match /dailyActivities/{date} {
        allow read, write: if isAuthenticated() && isOwner(userId);
      }
      
      // User activities subcollection
      match /activities/{activityId} {
        allow read, write: if isAuthenticated() && isOwner(userId);
      }
      
      // User activities subcollection (alternative naming)
      match /userActivities/{userActivityId} {
        allow read, write: if isAuthenticated() && isOwner(userId);
      }
      
      // User notifications subcollection
      match /notifications/{notificationId} {
        allow read, write: if isAuthenticated() && isOwner(userId);
      }
      
      // User points subcollection
      match /points/{pointId} {
        allow read, write: if isAuthenticated() && isOwner(userId);
      }
      
      // User achievements subcollection
      match /achievements/{achievementId} {
        allow read, write: if isAuthenticated() && isOwner(userId);
      }
      
      // User badges subcollection
      match /badges/{badgeId} {
        allow read, write: if isAuthenticated() && isOwner(userId);
      }
      
      // User environmental impact subcollection
      match /environmental_impact/{impactId} {
        allow read, write: if isAuthenticated() && isOwner(userId);
      }
      
      // User category points subcollection
      match /category_points/{categoryId} {
        allow read, write: if isAuthenticated() && isOwner(userId);
      }
    }
    
    // Global activities collection - all authenticated users can read
    match /activities/{activityId} {
      allow read: if isAuthenticated();
      allow write: if isAuthenticated() && isOwner(resource.data.userId);
    }
    
    // Global notifications collection
    match /notifications/{notificationId} {
      allow read, write: if isAuthenticated() && isOwner(resource.data.userId);
    }
    
    // Global points collection
    match /points/{pointId} {
      allow read, write: if isAuthenticated() && isOwner(resource.data.userId);
    }
    
    // Global achievements collection
    match /achievements/{achievementId} {
      allow read, write: if isAuthenticated() && isOwner(resource.data.userId);
    }
    
    // Global badges collection
    match /badges/{badgeId} {
      allow read, write: if isAuthenticated() && isOwner(resource.data.userId);
    }
    
    // Global environmental impact collection
    match /environmental_impact/{impactId} {
      allow read, write: if isAuthenticated() && isOwner(resource.data.userId);
    }
    
    // Global category points collection
    match /category_points/{categoryId} {
      allow read, write: if isAuthenticated() && isOwner(resource.data.userId);
    }
    
    // Deny all other access
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
